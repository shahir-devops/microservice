# services

10 Express microservices.

Each service should expose REST endpoints used by the React SPA routes:
- profile, cart, notifications, wishlist, orders, payments, helpcenter, address, reviews, auth

